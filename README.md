
  # Interactive Home Page UI

  This is a code bundle for Interactive Home Page UI. The original project is available at https://www.figma.com/design/Dx1o2oDnGTE0IEOarTnY5s/Interactive-Home-Page-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  "# PeerLearn" 
